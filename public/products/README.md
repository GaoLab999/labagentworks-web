# scRNA-agent product identity

`scrna-agent-logo.png` is copied unchanged from the approved asset at
`~/scrna-agent/workbench/static/assets/scrna-agent-logo.png` (1254 × 1254 PNG).
No approved SVG is present in that source directory.

SHA-256: `1f975dc175eb6f8f83f9bafdf71316ec4644d7d1d5d973787b9c2ff40975ffc2`

This mark identifies scRNA-agent only. Use it beside the product name, never as
the Lab Agent Works navigation, footer, favicon, or umbrella-brand identity.
The website serves its own local copy; there is no runtime cross-repository dependency.
The shared `ScRNAAgentLogo` component preserves the full image and aspect ratio.
It uses empty alt text because each placement has adjacent product-name text.
